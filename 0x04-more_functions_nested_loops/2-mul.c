#include "main.h"

/**
 * mul - multplies of a and b and store the value to mul
 * @a: an int number to be mulptiplie with b
 * @b: an int number to be multiplied by a
 * mul is the return product of a*b
 * Return: the product of a*b
 */

int mul(int a, int b)
{

int mul;
mul = a * b;
return (mul);
}
