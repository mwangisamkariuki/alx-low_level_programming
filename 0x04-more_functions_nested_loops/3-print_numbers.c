#include "main.h"

/**
 * print_numbers - the function print numbers from 0 to 9
 * char i is the character  that is being checked by the loop
 * mul is tvoid since it is only printing a loop
 */

void print_numbers(void)
{

char i;
for (i = '0' ; i <= '9'; i++)
_putchar(i);
_putchar('\n');
}
