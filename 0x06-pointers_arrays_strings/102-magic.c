#include <stdio.h>
/**
 * main - check the code for 102-magic.c
 * Remember:
 * - you are not allowed to use a
 * - you are not allowed to modify p
 * ..so that this prints 98\n
 * Return: 0 always
 */

int main(void)
{
int n;
int a[5];
int *p;

a[2] = 1024;
p = &n;
printf("a[2] = %d\n", a[2]);
return (0);
}
